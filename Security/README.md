# Security/ — _placeholder_

Security enforcement (ARCHITECTURE.md §14): offline-first (no telemetry by
default), least-privilege workspace-scoped access, network isolation for agent
tools, and Keychain-backed secret storage.

> Not yet a SwiftPM target — it becomes one when it has real code (see `Package.swift`).

# Persistence/ — _placeholder (Phase 2/3)_

SQLite-backed persistence (ARCHITECTURE.md §12): conversations, workspace
metadata, indexing state, model assignments, prompt history. Does not duplicate
repository contents or persist temporary inference buffers.

> Not yet a SwiftPM target — it becomes one when it has real code (see `Package.swift`).

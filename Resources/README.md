# Resources/ — _placeholder_

Bundled assets and model metadata. **No large model weights** — those download to
the Hugging Face cache at runtime and are git-ignored (see `.gitignore`).

> Not a SwiftPM target.
